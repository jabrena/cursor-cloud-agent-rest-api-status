<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.3.0/pml.xsd">
        
    <goal>
        Check internet connectivity using a curl command

        ```bash
        sudo apt update
        sudo apt install -y curl libcurl4
        cat /etc/resolv.conf
        sudo sed 's/nameserver 1.1.1.1/nameserver 8.8.8.8/g' /etc/resolv.conf | sudo tee /etc/resolv.conf > /dev/null
        grep -q "nameserver 8.8.8.8" /etc/resolv.conf || echo "nameserver 8.8.8.8" | sudo tee -a /etc/resolv.conf
        cat /etc/resolv.conf
        curl -L https://marca.com | grep "Marca"
        ```
    </goal>

    <output-format>Not explain anything only show the output of the curl command</output-format>
</prompt>
