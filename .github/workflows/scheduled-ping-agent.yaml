name: Scheduled Ping Agent

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'  # Run every hour at minute 0

jobs:
  scheduled-ping-agent:
    name: Scheduled Ping Agent Execution
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: true

      - name: Setup Java 25 with GraalVM
        uses: actions/setup-java@v5
        with:
          distribution: 'graalvm'
          java-version: '25'

      - name: Setup jbang
        run: |
          curl -Ls https://sh.jbang.dev | bash -s - app setup
          echo "$HOME/.jbang/bin" >> $GITHUB_PATH

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Execute AOC Agent and Measure
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          GITHUB_ACTIONS: true
        run: |
          chmod +x .github/scripts/execute-and-measure.sh
          ./.github/scripts/execute-and-measure.sh
